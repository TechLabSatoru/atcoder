package main

import (
	"fmt"

	"301/abc301"
)

func main() {

	/* This is code for A problem that is Overall Winner  */
	/*
	var N int
	var S string

	fmt.Scan(&N, &S)

	fmt.Println(abc301.OverallWinner(N, S))
	*/

	/* This is code for B problem that is Fill the Gaps */
	/*
	var N int

	fmt.Scan(&N)

	A := make([]int, N)

	for i := 0; i < N; i++ {
		fmt.Scan(&A[i])
	}

	fmt.Println(FillTheGaps(N, A))
	*/
}
